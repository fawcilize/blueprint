import consoleTest from "./sampleModule3";

const y = 2;
const z = 3;

export const nested = {
  testConsole: () => {
    consoleTest();
  }
};

export function testConsole() {
  consoleTest();
}
export { y, z };
export const w = -1;

export default x = 0;
